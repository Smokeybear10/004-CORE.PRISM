pydantic>=2.0
pytest>=7.0
python-dateutil>=2.8

# Ingestion
datasets>=2.0          # for JanosAudran/financial-reports-sec from HuggingFace
edgartools>=3.0        # SEC EDGAR live fetch (10-K fallback, 8-K primary)
huggingface_hub>=0.20  # direct parquet download for bundled dataset (news)
beautifulsoup4>=4.12   # strip HTML/XBRL from raw 8-K filings
lxml>=4.9              # bs4's preferred parser for HTML/XML
requests>=2.31
tiktoken>=0.5          # accurate token counting for chunking

# Prices & market data
yfinance>=0.2
pandas>=2.0
numpy>=1.24

# Model (anthropic for attribution)
anthropic>=0.30

# Backtest & demo
matplotlib>=3.7
jupyter>=1.0
streamlit>=1.30
